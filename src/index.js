import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('dummyID'));
console.log("indexxxxxxs")
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
